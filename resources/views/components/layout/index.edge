@vite(['resources/css/app.css'])
<!DOCTYPE html>
<html>
<div class="text-lg mx-auto">

    <head>
        @include('partials/nav.edge')
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        @if ($slots.meta)
        {{{ await $slots.meta() }}}
        @endif
        <title>{{ title || "Let's Learn AdonisJS 6"}}</title>
    </head>

    <body>

    {{{ await $slots.main() }}}

    </body>
</div>